package homework5;

public class exampleTen {
    public static void main(String[] args) {

        double length = 5.7 ;
        double width = 2.9;

        double area = length * width;

        double perimeter = 2 * (length + width);

        System.out.print("The area of the rectangle of length " + length + " and width " + width + " is " + area + " and the perimeter of the rectangle is " + perimeter);
        
    }
}